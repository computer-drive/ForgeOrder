# MAIN
本文档介绍了`class_name`为`MAIN`时，各`method`的日志记录。

## 1. `DebugMsg`
 - 级别: `DEBUG`
 - 调试日志，`msg`为普通文本。

## 2. `StartServer`
 - 级别: `INFO`
 - 启动服务器日志，`msg`为json格式，包含服务器地址、端口号。

## 3. `ServerStopped`
 - 级别: `INFO`
 - 服务器停止日志，无`msg`。

 
 
